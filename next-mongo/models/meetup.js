// import mongoose, { Schema } from 'mongoose'

// const MODEL_NAME = 'Meetup'

// const schema = new Schema({
//     title: {
//         type: String,
//         required: true
//     },
//     description: {
//         type: String,
//         required: true,
//     },

// },
//     {
//         timestamps: true,
//     }
// )

// // export default mongoose.model(MODEL_NAME, schema, 'meetups')
// // export default (mongoose.models && mongoose.models.MODEL_NAME
// //     ? mongoose.models.MODEL_NAME
// //     : mongoose.model(MODEL_NAME, schema));

// export default mongoose.models.MODEL_NAME || mongoose.model(MODEL_NAME, schema)
